<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================= -->
    <!-- Networking (raw TCP over java.net sockets)                    -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ============================================================= -->
    <!-- Audio capture. AudioPlaybackCapture rides on the MediaProjection -->
    <!-- consent flow; RECORD_AUDIO is requested at runtime as well.      -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ============================================================= -->
    <!-- Foreground services. On targetSdk 34+ every FGS type needs its   -->
    <!-- own manifest permission in addition to FOREGROUND_SERVICE.       -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Status notifications shown by the foreground services.
         Runtime permission on Android 13 (API 33) and newer. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameAudioStream">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sender.SenderActivity"
            android:exported="false" />

        <activity
            android:name=".receiver.ReceiverActivity"
            android:exported="false" />

        <!-- Sender: captures internal game/media audio.
             Android 14+ requires startForeground(FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION)
             to be called BEFORE MediaProjectionManager.getMediaProjection(). -->
        <service
            android:name=".sender.SenderService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Receiver: plays the incoming PCM stream through an AudioTrack. -->
        <service
            android:name=".receiver.ReceiverService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
